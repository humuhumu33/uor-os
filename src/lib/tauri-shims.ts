/**
 * Empty shims for Tauri plugins — used in web builds so Rollup
 * can resolve the bare imports without externalizing them.
 * Actual Tauri code-paths should guard with `isLocal()` at runtime.
 */
export {};
