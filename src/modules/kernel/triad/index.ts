/**
 * triad module — ABSORBED into ring-core.
 * This barrel re-exports from ring-core/triad for backward compatibility.
 * @deprecated Import from @/modules/ring-core instead.
 */
export { computeTriad, popcount, basisElements, stratumLevel, stratumDensity } from "@/modules/kernel/ring-core/triad";
