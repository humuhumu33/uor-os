# Interoperability

Standards compatibility and API exploration tools.

## Modules

| Module | Description |
|--------|-------------|
| `cncf-compat/` | CNCF (Cloud Native Computing Foundation) compatibility layer |
| `api-explorer/` | Interactive API explorer for the UOR bus |

Existing interoperability module code (protocol bridges, format converters) lives alongside these directories.
